name: Market Open (8:30 AM CT)

on:
  schedule:
    - cron: "30 13 * * 1-5"   # 13:30 UTC = 8:30 AM CDT (UTC-5)
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install anthropic requests yfinance discord.py

      - name: Run market open routine
        env:
          ANTHROPIC_API_KEY:   ${{ secrets.ANTHROPIC_API_KEY }}
          ALPACA_API_KEY:      ${{ secrets.ALPACA_API_KEY }}
          ALPACA_SECRET_KEY:   ${{ secrets.ALPACA_SECRET_KEY }}
          TAVILY_API_KEY:      ${{ secrets.TAVILY_API_KEY }}
          DISCORD_BOT_TOKEN:   ${{ secrets.DISCORD_BOT_TOKEN }}
          DISCORD_USER_ID:     ${{ secrets.DISCORD_USER_ID }}
        run: python runner.py market_open

      - name: Commit updated memory files
        run: |
          git config user.name  "Trading Bot 🤖"
          git config user.email "bot@trading-ai"
          git add memory/ strategy/ config/
          git diff --staged --quiet || git commit -m "📈 market_open — $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
